-- DISTINCT Examples

-- Show unique cities
SELECT DISTINCT city
FROM customers;

-- Show unique customer names
SELECT DISTINCT customer_name
FROM customers;

-- Show unique ages
SELECT DISTINCT age
FROM customers;
